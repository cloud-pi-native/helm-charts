{{- range .Values.application.repositories }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.argocd.project }}-{{ .name }}-{{ sha1sum .repoURL | trunc 4 }}
  namespace: {{ $.Values.argocd.namespace }}
  labels:
    app.kubernetes.io/managed-by: dso-console
    {{- $.Values.common | toYaml | nindent 4 }}
    dso/repository: {{ .name }}
    dso/repository.id: {{ .id }}
    dso/type: app
  annotations:
    argocd.argoproj.io/sync-wave: "4" # Force la suppression avant le AppProject
  finalizers:
    # foreground cascading deletion
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ $.Values.argocd.project }}-app
  {{- $repo := . }}
  {{- if or .valueSources .chart }}
  {{- $externalSources := list }}
  {{- range .valueSources }}
    {{- if and $repo.chart (eq .type "internal") }}
      {{- fail (printf "repository %q : les values de type 'internal' ne sont pas supportées pour un chart OCI/Helm (%s/%s), utilisez une source de type 'external'" $repo.name $repo.repoURL $repo.chart) }}
    {{- end }}
    {{- if eq .type "external" }}
      {{- $externalSources = append $externalSources . }}
    {{- end }}
  {{- end }}
  sources:
    - repoURL: {{ .repoURL }}
      targetRevision: {{ .targetRevision }}
      {{- if .chart }}
      chart: {{ .chart }}
      {{- else }}
      path: {{ .path }}
      {{- end }}
      {{- if .valueSources }}
      helm:
        valueFiles:
          {{- range .valueSources }}
          {{- if eq .type "internal" }}
          - {{ .path }}
          {{- else if eq .type "external" }}
          - ${{ .ref }}/{{ .path }}
          {{- end }}
          {{- end }}
      {{- end }}
    {{- range $externalSources }}
    - repoURL: {{ .repoURL }}
      targetRevision: {{ .targetRevision }}
      ref: {{ .ref }}
    {{- end }}
  {{- else }}
  source:
    repoURL: {{ .repoURL }}
    targetRevision:  {{ .targetRevision }}
    path: {{ .path }}
    {{- if len .valueFiles }}
    helm:
      valueFiles: {{ .valueFiles | toJson }}
    {{- end }}
  {{- end }}
  destination:
    name: {{ $.Values.application.destination.name }}
    namespace: {{ $.Values.application.destination.namespace }}
  {{- $autoSync := .autosync | default $.Values.application.autosync -}}
  {{- if $autoSync }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
  {{- end }}
{{- end }}
